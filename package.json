{
  "name": "chore-wheel",
  "version": "0.0.1-alpha",
  "description": "An application to maintain a chore wheel",
  "main": "index.js",
  "scripts": {
    "coverage": "jest --no-cache --coverage",
    "lint": "eslint . --ext ts,d.ts,js",
    "lint-fix": "eslint . --ext ts,d.ts,js --fix",
    "test": "jest",
    "transpile": "tsc --build tsconfig.json",
    "validate": "npm t && npm run lint"
  },
  "author": "Francis Sirizzotti",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/jest": "^24.0.17",
    "@typescript-eslint/eslint-plugin": "^1.13.0",
    "@typescript-eslint/parser": "^1.13.0",
    "eslint": "^6.1.0",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2"
  },
  "dependencies": {
    "typescript": "^3.5.3"
  }
}
